const express = require('express');
const router = express.Router();

// Placeholder - Will implement in Week 1
router.get('/', (req, res) => {
  res.json({ message: 'Users routes - Coming soon' });
});

module.exports = router;
